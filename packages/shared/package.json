{
  "name": "@your-harness/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types, contracts and utilities for Your Harness.",

  "type": "module",

  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",

  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },

  "files": [
    "dist"
  ],

  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "clean": "rimraf dist"
  },

  "dependencies": {},

  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
